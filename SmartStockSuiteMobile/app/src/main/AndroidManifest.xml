<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:usesCleartextTraffic="true"
        android:icon="@mipmap/ic_launcher"
        android:label="SmartStockSuite Mobile"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SmartStockSuiteMobile">

        <activity
            android:name=".activities.BarcodeScannerActivity"
            android:exported="false" />

        <activity
            android:name=".activities.MobileSummaryActivity"
            android:exported="false" />

        <activity
            android:name=".activities.InventoryScannerActivity"
            android:exported="false" />

        <activity
            android:name=".activities.PickingTasksActivity"
            android:exported="false" />

        <activity
            android:name=".activities.PickerHomeActivity"
            android:exported="false" />

        <activity
            android:name=".activities.LoginActivity"
            android:exported="false" />

        <activity
            android:name=".MainActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

    </application>

</manifest>